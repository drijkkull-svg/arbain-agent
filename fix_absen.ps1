$file = "C:\arbain_agent\lib\screens\home_screen.dart"
$content = [System.IO.File]::ReadAllText($file)
$old = "          // MENU GRID"
$new = "          // CARD ABSEN NGAJI`n          if (_isNgajiTime) ...[`n            Container(`n              width: double.infinity,`n              padding: const EdgeInsets.all(16),`n              margin: const EdgeInsets.only(bottom: 16),`n              decoration: BoxDecoration(`n                color: const Color(0xFF111111),`n                borderRadius: BorderRadius.circular(16),`n                border: Border.all(color: Colors.orange.withValues(alpha: 0.5)),`n              ),`n              child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [`n                const Row(children: [`n                  Icon(Icons.menu_book, color: Colors.orange, size: 16),`n                  SizedBox(width: 8),`n                  Text('ABSEN NGAJI', style: TextStyle(color: Colors.orange, fontSize: 11, letterSpacing: 1.5, fontWeight: FontWeight.bold)),`n                ]),`n                const SizedBox(height: 12),`n                _sudahAbsen`n                  ? const Row(children: [`n                      Icon(Icons.check_circle, color: Color(0xFF00FF88), size: 20),`n                      SizedBox(width: 8),`n                      Text('Sudah Absen', style: TextStyle(color: Color(0xFF00FF88), fontSize: 14, fontWeight: FontWeight.bold)),`n                    ])`n                  : SizedBox(`n                      width: double.infinity,`n                      child: ElevatedButton.icon(`n                        onPressed: () async {`n                          final ok = await _absensiService.absen();`n                          if (ok && mounted) setState(() => _sudahAbsen = true);`n                        },`n                        icon: const Icon(Icons.how_to_reg, size: 18),`n                        label: const Text('Tandai Hadir Ngaji'),`n                        style: ElevatedButton.styleFrom(`n                          backgroundColor: Colors.orange,`n                          foregroundColor: Colors.black,`n                          padding: const EdgeInsets.symmetric(vertical: 12),`n                          shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(10)),`n                        ),`n                      ),`n                    ),`n              ]),`n            ),`n          ],`n          // MENU GRID"
$content = $content.Replace($old, $new)
[System.IO.File]::WriteAllText($file, $content, [System.Text.Encoding]::UTF8)
Write-Host "SUCCESS"
